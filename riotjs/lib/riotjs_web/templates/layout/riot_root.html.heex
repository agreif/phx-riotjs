<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="utf-8"/>
    <meta http-equiv="X-UA-Compatible" content="IE=edge"/>
    <meta name="viewport" content="width=device-width, initial-scale=1.0"/>
    <meta name="csrf-token" content={csrf_token_value()}>
    <%= live_title_tag assigns[:page_title] || "Riotjs", suffix: " · Phoenix Framework" %>
    <link phx-track-static rel="stylesheet" href={Routes.static_path(@conn, "/assets/app.css")}/>
    <script defer phx-track-static type="text/javascript" src={Routes.static_path(@conn, "/assets/app.js")}></script>
    <script type="module">
      <%= for tag <- @riot_tags do %>
        <% tag_str = Atom.to_string tag %>
        import <%= tag_str %>Tag from "/riot/<%= tag %>_tag.js"
        riot.register("<%= tag %>-tag", <%= tag_str %>Tag)
      <% end %>
      <%= for page <- @riot_pages do %>
        <% page_str = Atom.to_string page %>
        import <%= page_str %>Page from "/riot/<%= page %>_page.js"
        riot.register("<%= page %>-page", <%= page_str %>Page)
      <% end %>
      riot.mount('body-tag')[0].refreshData("<%= @data_path %>")
    </script>
  </head>
  <body>
    <%= @inner_content %>
  </body>
</html>
